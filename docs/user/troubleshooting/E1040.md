# E1040 Details

## Message emitted:

`E1040`: Facts getter failed during schema validation on `{hostname}`: `{exception}`.

## Description:

Facts getter failed during schema validation on `{hostname}`.

## Troubleshooting:

Ensure correct schema is used.

## Recommendation:

Verify that formatted data and details match the schema used.