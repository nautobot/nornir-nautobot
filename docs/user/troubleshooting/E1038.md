# E1038 Details

## Message emitted:

`E1038`: Facts getter failed during parse_command_result on `{hostname}`: `{exception}`.

## Description:

Facts getter failed during parse_command_result on `{hostname}`.

## Troubleshooting:

Ensure correct filtering and parsing configuration is used.

## Recommendation:

Verify that Jinja2 filters are available and correct JPath is used.